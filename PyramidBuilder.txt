// Pyramid Builder
// by: myithspa25
// (Eventually) a port of standardcombo's Pyramid Builder game into StoneScript.
// Beware: probably terrible code ahead.

// TODO: make code
?loc = Waterfall & !loc.isQuest
    //draw.Clear()
    >`15,1,Version: Test 3 | branch: fix-button-position
    func ExitButton()
        loc.Leave()
    ?loc.begin
        draw.Clear()
        var exit = ui.AddButton()
        exit.y = (screen.h/2)-4
        exit.x = (-screen.w/2)+9
        exit.text = "Exit"
        exit.SetPressed(ExitButton)