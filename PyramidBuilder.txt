// Pyramid Builder
// by: myithspa25
// (Eventually) a port of standardcombo's Pyramid Builder game into StoneScript.
// Beware: probably terrible code ahead.

// TODO: make code
?loc = Waterfall & !loc.isQuest
    //draw.Clear()
    >`15,1,Version: Test 2 | branch: fix-button-position
    func ExitButton()
        loc.Leave()
    ?loc.begin
        var exit = ui.AddButton()
        exit.y = -5
        exit.x = -screen.w/2 //why does this throw an error? "Attempting to access member variable on non-object"
        exit.text = "Exit"
        exit.SetPressed(ExitButton)